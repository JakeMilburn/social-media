{% extends 'base.html.twig' %}

{% block title %}Users Profile{% endblock %}

{% block body %}
    <style>
        .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
        .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
    </style>

    <div class="example-wrapper">


        {% for user in users %}

           <h2>{{ user.username }}</h2>
            <img src="../css/files/{{ user.username }}/{{ user.path }}">

        {% endfor %}

        {% for post in posts %}

            {{ post.title }}
            {{ post.body }}

        {% endfor %}

        <br/>
        This friendly message is coming from:
        <ul>
            <li>Your controller at <code><a href="{{ '/Applications/MAMP/htdocs/social-media/src/Controller/FeedController.php'|file_link(0) }}">src/Controller/FeedController.php</a></code></li>
            <li>Your template at <code><a href="{{ '/Applications/MAMP/htdocs/social-media/templates/feed/index.html.twig'|file_link(0) }}">templates/feed/index.html.twig</a></code></li>
        </ul>
    </div>
{% endblock %}
