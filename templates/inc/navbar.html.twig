<nav class="navbar">
    <div class="container">
        <a href="/media/public/" class="navbar-brand">A title</a>
        <div>
            <ul class="navbar-nav">
                <li class="nav-item">
                    <a href="/social-media/public/" class="nav-link">News Feed</a>
                </li>
                {% if is_granted("ROLE_USER") %}
                    <li class="nav-item">
                        <a href="/social-media/public/profile/{{ app.user.id }}" class="nav-link">Profile</a>
                    </li>
                {% endif %}
                {#{% if is_granted('ROLE_ADMIN') %}#}
                    {#<li class="nav-item">#}
                        {#<a href="{{ path('new_sweet') }}" class="nav-link">New Sweet</a>#}
                    {#</li>#}
                    {#<li class="nav-item">#}
                        {#<a href="{{ path('manage') }}" class="nav-link">Manage Users</a>#}
                    {#</li>#}
                    {#<li class="nav-item">#}
                        {#<a href="{{ path('logout') }}" class="nav-link">Logout</a>#}
                    {#</li>#}
                {#{% elseif is_granted('ROLE_USER') %}#}
                    {#<li class="nav-item">#}
                        {#<a href="{{ path('logout') }}" class="nav-link">Logout</a>#}
                    {#</li>#}
                {#{% else %}#}
                    {#<li class="nav-item">#}
                        {#<a href="{{ path('login') }}" class="nav-link">Login</a>#}
                    {#</li>#}
                {#{% endif %}#}
            </ul>

        </div>
        <form action="{{ path('search') }}" method="post">
            <input type="text" name="search_criteria" placeholder="Search for a user">
            <button type="submit" class="btn btn-primary mt-3">Search</button>
        </form>
    </div>
</nav>